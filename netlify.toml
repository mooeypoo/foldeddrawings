[build]
  command = "npm run build"
  publish = "dist"

# Redirect rules for SPA-like behavior (optional)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 404

# Form submissions will be automatically handled by Netlify
# No additional configuration needed for forms
